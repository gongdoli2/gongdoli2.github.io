---
---

<div class="not-found">
    <div class="icon">
        <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#654DD8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="12" y1="8" x2="12" y2="12"></line>
            <line x1="12" y1="16" x2="12" y2="16"></line>
        </svg>
    </div>

    <div class="content">
        <h1>404</h1>
        <h2>Page Not Found</h2>
        <p>죄송합니다. 찾으시는 페이지가 존재하지 않습니다.</p>
        <a href="/" class="home-btn button-common">홈으로 돌아가기</a>
    </div>
</div>

<style>

    .not-found {
        min-height: calc(100vh - 50px);
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        height: 100vh;
        text-align: center;
        color: white;
        opacity: 0;
        transform: translateY(-30px);
        transition: all 1s ease;
    }

    .not-found.show {
        opacity: 1;
        transform: translateY(0);
    }

    .icon {
        margin-bottom: 2rem;
        animation: float 2s ease-in-out infinite;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    .content h1 {
        font-size: 4rem;
        margin: 0;
        color: #654DD8;
        animation: pop 0.6s ease;
    }

    .content h2 {
        font-size: 2rem;
        margin: 1rem 0;
        color: #fff;
        opacity: 0;
        animation: fadeInUp 0.6s ease forwards;
        animation-delay: 0.3s;
    }

    .content p {
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
        opacity: 0;
        animation: fadeInUp 0.6s ease forwards;
        animation-delay: 0.5s;
    }

    @keyframes pop {
        0% { transform: scale(0); opacity: 0; }
        60% { transform: scale(1.2); opacity: 1; }
        100% { transform: scale(1); }
    }

    @keyframes fadeInUp {
        0% { opacity: 0; transform: translateY(20px); }
        100% { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
        .content h1 { font-size: 4rem; }
        .content h2 { font-size: 1.5rem; }
        .content p { font-size: 1rem; }
    }
</style>

<script>
    window.addEventListener("DOMContentLoaded", () => {
        const notFound = document.querySelector(".not-found");
        if (notFound) {
            setTimeout(() => {
                notFound.classList.add("show");
            }, 200);
        }
    });
</script>
