---
import SideBar from "../../layouts/SideBar.astro";
---

<SideBar/>
<section class="home">
    <div class="img-home">
        <img src="/assets/about/team-logo.png" alt="team-logo"/>
    </div>
    <div class="info">
        <h1><span class="highlight">공돌이</span></h1>
        <h3>고등학생 코딩 동아리</h3>
        <p><span class="highlight">아이디어</span>를 현실로 만드는 동아리입니다.</p>
        <p>코딩에 관심 있는 누구나 자유롭게 참여할 수 있으며, 혼자서는 어려운 프로젝트도 팀원들과 함께라면 실현할 수 있습니다.</p>
        <p>저희는 <span class="highlight">게임, 앱, 웹, 로봇</span> 등 다양한 분야의 프로젝트를 진행하며, 실제 동작하는 결과물을 만들어보는 것을 목표로 합니다.</p>
        <p>단순히 코딩 실력뿐만 아니라 <span class="highlight">문제 해결 능력, 협업 능력, 창의적 사고</span>까지 키울 수 있는 공간입니다.</p>
    </div>
</section>

<style>
    .highlight {
        color: #fffacf;
        text-shadow: 0 2px 4px rgba(0,0,0,0.2);
        font-weight: 600;
    }

    .home {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 4rem;
        padding: 60px 5%;
        opacity: 0;
        transform: translateY(-40px);
        transition: all 1s ease;
    }

    .home.show {
        opacity: 1;
        transform: translateY(0);
    }

    .img-home {
        width: 250px;
        height: 250px;
        border-radius: 50%;
        overflow: hidden;
        box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        flex-shrink: 0;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .img-home:hover {
        transform: scale(1.05);
        box-shadow: 0 15px 25px rgba(0,0,0,0.25);
    }

    .img-home img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 50%;
        transition: transform 0.3s ease;
    }

    .info {
        max-width: 500px;
    }

    .info h1 {
        font-size: 3rem;
        font-weight: 700;
        line-height: 1.1;
        margin-bottom: 0.5rem;
    }

    .info h3 {
        font-size: 1.6rem;
        font-weight: 500;
        color: #e0e0e0;
        margin-bottom: 1rem;
    }

    .info p {
        font-size: 1rem;
        line-height: 1.6;
        color: #dcdcdc;
        margin-bottom: 0.8rem;
    }

    @media (max-width: 900px) {
        .home {
            flex-direction: column;
            gap: 1.5rem;
            text-align: center;
            padding: 30px 5%;
        }

        .img-home {
            width: 150px;
            height: 150px;
        }

        .info h1 {
            font-size: 1.8rem;
            line-height: 1.2;
        }

        .info h3 {
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .info p {
            font-size: 0.9rem;
            margin: 0.4rem 0;
        }
    }
</style>

<script>
    window.addEventListener("load", () => {
        const home = document.querySelector(".home");
        home?.classList.add("show");
    });
</script>
